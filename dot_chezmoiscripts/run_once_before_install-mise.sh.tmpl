#!/bin/bash
set -eu
# macOS installs mise via brew bundle; this only covers Linux.
{{ if and (dig "mise" false .) (ne .chezmoi.os "darwin") -}}
command -v mise >/dev/null 2>&1 && exit 0
curl -fsSL https://mise.run | sh
{{ end -}}
